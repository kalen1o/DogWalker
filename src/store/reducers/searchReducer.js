const initialState = {
	searchParam: {
		services: "Dog Boarding",
		city: "Kharkiv",
		// startDate: "",
		// endDate: "",
		// dogSizes: "",
		// radio: "One Time",
		// days: []
	}
}

const searchReducer = (state = initialState, action) => {
	return state;
}

export default searchReducer;